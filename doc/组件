1，gate（p总）
	给用户分配front节点
	挂掉没关系
2，front（s小弟）
	管理客户端连接
	上报用户信息到session
	挂掉客户端重连
3，session（s小弟）
	保存user->front信息
	挂掉front重新上报
4，user（p总）
	用户资料缓存
	私聊消息发送
	挂掉没关系
5，group（p总）
	群组资料缓存， 根据群组id hash到不同的服务器
	群聊消息发送
	半状态，挂掉没关系
6，rest（s小弟）
	外部接口
	单聊消息分配到user，群聊消息分配到group
	无状态，挂掉没关系